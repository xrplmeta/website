button.back{
	background: transparent;
	border: none;
	outline: none;
	display: flex;
	align-items: center;
	gap: 10px;
	font-family: inherit;
	font-size: inherit;
	margin-top: 25px;
	font-weight: 500;

	&:active{
		opacity: 0.7;
	}
}